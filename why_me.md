# why_me：让 AI 打工，而不是你陪 AI 打工

用 Claude Code 干正事，最贵的不是 token，是你守在键盘前的时间。worker 会话停了要你敲"继续"，限流了要你等完了再催，干完一段要你亲自检查才敢放行下一段。说是 AI 在干活，其实你一直在值夜班。

这套东西把夜班撤了。一个 supervisor 会话管 N 个 worker 会话：你写好目标、答完开局的需求澄清，就可以走开。剩下的流转、验收、中断自愈，监工自己跑，只在需要你拍板的时候弹通知找你。

## 它凭什么让你走得开

**限流自愈，不用敲"继续"。** 多 worker 并行跑，集体撞 429 是常态。worker 被掐断的瞬间，StopFailure/Stop hook（事件回调，不是 prompt 叮嘱）直接把中断报给监工。监工记账、退避五分钟、逐个错峰唤醒（每多一个 worker 顺延一分钟），恢复后从断点继续。你从头到尾不用出现。

**逐 phase 自动流转。** worker 干完一个 phase，先自审，再交监工对抗审查；过了，监工自动下发下一个 phase 的完整指令；不过，带着问题清单打回重做。流水线自己往前走，卡住了才升级到你。

**开局把问题问完，后面不烦你。** 动工前监工会把模糊目标盘成 spec 和 plan，该问的需求问题集中在这一段问掉。之后它只在两种情况下找你：需要你拍板的决策（QUESTIONS 协议，异步攒着），和它自己处理不了的事故（桌面通知直投你）。其余时间，你不在场是常态，不是失职。

**崩了能接上，不用重来。** 全部进度落盘在 .supervisor/ 的分片账本里。worker 崩了重开一个会话，对齐账本接着干；监工自己崩了，resume 回来同样接上。你的投入不存在"一夜回到解放前"这个选项。

## 四个模式，各替你挡一类坑

绿地新项目（/supervisor）：AI 从零写项目最大的风险是理解偏了你没发现，等看到成品已经晚了。监工开局把目标拷问成 spec 和 plan 给你确认，之后逐 phase 验收，偏了当场打回。你的参与收敛为开局二十分钟加收尾看报告。

老项目改造（/rework）：AI 改老代码最吓人的是"顺手"——顺手重构、顺手改测试、顺手动了你明令不让动的东西。监工先考古出基线，立回归安全网，列不改清单，每个 phase 结束用 git 双重比对范围，越界即返工。

调研（/research）：AI 调研最大的坑是一脸自信的幻觉。证据分五级标注，监工亲自抽查复现关键证据，伪证一条整章重查。你拿到的报告里，每句话能追溯到来源。

提炼（/abstract）：喂给它一堆材料让它总结，它要么给你"正确的废话"，要么写着写着偷偷加材料。材料清单定稿即锁死，覆盖对账保证每份材料被解释或显式标反例，证伪不了又用不上的命题会被降级。

## 为什么信得过

这套东西的每个防御层后面都躺着一次真实翻车：worker 429 静默死亡无人知晓，才有了 hook 级中断上报；监工自己劣化三个半小时无任何信号（2026-09-07 实录），才有了进程外 watchdog 盯监工心跳——它跑在系统 crontab 里，监工病了它照样弹通知。

质量侧：三个测试脚本 160 多条断言；abstract 模式 81 分钟真机双会话冒烟 15 项断言全过；research 模式在真实生产仓跑完 11 个 phase 审计；合入 main 前过了三轮独立 code review，13 个问题全部修复。

## 先说清楚边界

监工不能保证永不出错，它的人格来自 prompt，遵循度不是百分之百。整套设计的目标是把最坏后果从"不发现"压缩成"晚发现"。桌面通知用 osascript，只在 macOS 响。单个 prompt 能干完的小活，别用这套。

## 30 秒开始

```bash
curl -fsSL https://raw.githubusercontent.com/ljheee/claude-supervisor/main/install.sh | sh
```

终端 A：`/rename supervisor`，然后 `/supervisor 做一个XXX功能`。答完它开局的问题。
终端 B（同一项目目录）：`/worker`。

然后去干你自己的事。该找你的时候，它会弹通知。
