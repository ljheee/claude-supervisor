# 为什么写这套监工

我在 Claude Code 里开一个会话当监工（supervisor），督促和审查另外 N 个干活的会话（worker）。听起来像在给自己招员工，真正的麻烦也确实从这里开始：AI 员工不是不会干活，是会悄悄死掉、悄悄跑偏，而你毫不知情。

## 经常遇到的问题

最常见的是 worker 静默死亡。429 限流、网络断开、进程被杀，这三种情况下 worker 自己不会发出任何消息。死掉的东西没法报丧，这不是它的错，但你两小时后回到终端，只能对着一片沉默猜进度。

其次是监工自己会坏。9 月 7 号那次事故我印象很深：监工会话劣化了三个半小时，零产出，但进程还活着。定时巡检的 cron 属于它自己的会话，它病了，tick 跟着哑。整整三个半小时里没有任何机制能发现这件事，因为所有的检查者都是它自己。

第三，prompt 里写的"自觉"靠不住。我试过在协议里写"记得巡检""别动产品代码""干完再报"，模型大多数时候遵守，偶尔不遵守。状态漂移过、把测试改绿充数过。软约束的问题在于你没法知道它这次守不守。

还有多监工互相踩踏。A 监工收尾时顺手把 watchdog 的 crontab 拆了，B 监工的外部防御跟着消失，B 自己不知道。两个监工往同一份账本里写，内容互相覆盖。

## 怎么解决的

我的思路只有一条：凡是依赖模型自觉的保证，都在外面补一层不依赖它的硬代码。

中断检测做成了 hook，不是 prompt。StopFailure 和 Stop 是 Claude Code 的事件回调，worker 的回合被 429 或网络错误掐断时，hook 直接通过 Unix socket 把 WORKER INTERRUPTED 投递给监工，同时在项目的 .supervisor/ 里落一份 interrupts.jsonl。报丧这件事不再依赖死者本人。

盯梢的人不能是同伙。watchdog 是个两百多行的 shell 脚本，注册在系统 crontab 里每 10 分钟跑一次。它只认两个硬信号：worker 最后一次主动汇报的时间戳，监工在 registry 里的心跳。静默超时就用 osascript 弹桌面通知，直接找用户。监工劣化时它的 cron 也是哑的，所以这个检查者必须在进程外。

进度不放在对话里。所有状态落盘在 .supervisor/ 下，按监工的 session_id 分成互不干扰的片。worker 中断后恢复，先对齐账本再继续；监工失忆，resume 回来同样靠账本接上。分片还有一个附带好处：多个监工在同一项目里并存，谁也写不脏谁的账。

分发用拼接。每种模式（绿地、rework、research、abstract）是一个几十行的薄壳，共享的 195 行核心协议由 install.sh 在安装期拼进去，拼完过结构断言。改核心协议只改一处，四个模式同时生效。

要承认边界：监工的人格仍然来自 prompt 注入，遵循度做不到百分之百。这套东西的对冲思路是把最坏后果从"不发现"压缩成"晚发现"，而不是承诺监工永不犯错。

## 支持哪种场景

绿地新项目用 /supervisor。监工先把需求盘成 spec 和 plan，然后几个终端各开一个 /worker 并行做不同模块，每轮产出先自审再受审。

老项目动刀用 /rework。动手前先考古基线，立回归安全网，列不改清单，逐 phase 推进，每 phase 结束做范围比对，越界即返工。

调研用 /research。产出报告和证据链，产品代码只读，证据分五级标注置信度，非 git 目录也能跑。

提炼用 /abstract。给一堆现成材料，文档也好 diff 也好，口述背景也行，产出支配这些材料的高层命题。材料清单定稿即锁死，防止边写边偷偷扩面。

同一项目可以同时跑绿地和 rework 两个监工，账本分片互不污染。单个 prompt 就能干完的小活不需要这套东西，杀鸡用牛刀纯属浪费。

## 装它

```bash
curl -fsSL https://raw.githubusercontent.com/ljheee/claude-supervisor/main/install.sh | sh
```

装到 ~/.claude 下，不碰别处。已有文件先备份再覆盖，settings.json 损坏时中止安装而不是重置你的配置。
