# why_me：什么时候该用它

这是给 Claude Code 用的多会话监工套件：一个 supervisor 会话管着 N 个 worker 会话，worker 干活，supervisor 验收、督促、兜底。如果你只开一个会话让 AI 从头干到尾，不需要它；如果你开过三四个终端让 AI 并行干活，往下读。

## 你可能已经吃过这些亏

开三个 worker 并行干活，其中一个被 429 掐断。它不会告诉你，沉默是它唯一能发出的信号。一小时后你切过去，它停在半个 phase 上，进度全靠翻对话记录考古。

让一个会话连干两小时，它的上下文开始发糊：早期定的约束忘了，文件结构记岔了，越写越错。没有独立的验收方，自己检查自己等于没检查。

在 prompt 里写"记得自检""别动产品代码"，模型答应得很干脆。守不守，看运气。

两个并行任务各起了一个监工，收尾时一个顺手把共享的 crontab 清理了，另一个的外部告警跟着消失。文件账本互相覆盖，谁都不知道。

会话崩了想恢复，发现进度全在对话里。resume 回来，要么从头再干，要么靠 AI 自己"回忆"——而它的回忆经常是编的。

## 凭什么选它，而不是别的做法

**对比单会话硬扛。** 这套把干活的和验收的拆成两个会话。worker 的上下文随便糊，验收方是干净的独立上下文，只看产出物。worker 崩了，重开一个会话对齐账本接着干，损失有上界。

**对比官方 Task / subagent。** 那是同一个会话内的派生，父会话要养着全部子上下文，worker 出事没有跨会话的恢复通道。这套是真·多进程：每个 worker 是独立的 claude 进程，独立上下文，独立可 resume，消息走官方 cross-session 通道。挂着也一样。

**对比编排框架。** LangGraph 那类要你写代码、跑服务、维护状态机。这套是 prompt 协议加四个 hook 加一个 cron 脚本，一条命令装完，你照常敲 slash 命令，没有一行代码要写。

四个机制是别处很难凑齐的：

- 中断上报走 Claude Code 的 StopFailure/Stop 事件回调，不是 prompt 叮嘱。worker 被 429 或网络错误掐断时，hook 把 WORKER INTERRUPTED 直投给监工，同时落盘 interrupts.jsonl。报丧不依赖死者本人。
- watchdog 跑在系统 crontab 里，每 10 分钟查一次 worker 静默和监工心跳，超时直接弹桌面通知给用户。检查者不在被检查者的会话里，监工病了它照样响。
- 账本全部落盘在 .supervisor/ 下，按监工的 session_id 分片。多监工并存互不污染，PreToolUse hook 会机械拦截写错分片的行为。
- 四种模式共享一套 195 行核心协议，由 install.sh 在安装期拼接生成，改一处四处生效。

## 设计都有事故出处

这套东西不是坐着设计出来的，每个防御层背后都有一次真实翻车：worker 429 静默死亡无人知晓，才有了 hook 级中断上报；监工自己劣化三个半小时无任何信号（2026-09-07 实录），才有了进程外 watchdog 盯监工心跳；多监工互拆台，才有了分片账本和"先查他人再拆 cron"的收尾纪律。

质量侧的账：三个测试脚本共 160 多条断言；abstract 模式经过 81 分钟真机双会话冒烟，15 项断言全过；research 模式在真实生产仓跑完了 11 个 phase 的代码审计。三轮独立 subagent code review 抓出 13 个问题，全部修复后才合入 main。

## 也说清楚什么情况别用

单个 prompt 能干完的小活，别上这套，纯增加复杂度。

它不能保证监工永不出错。监工的人格来自 prompt 注入，遵循度做不到百分之百。整套设计的目标是把最坏后果从"不发现"压缩成"晚发现"，仅此而已。

桌面通知用的 osascript，只在 macOS 上响。Linux 下其余功能（hook、UDS、cron）正常，通知要自己改。

## 30 秒开始

```bash
curl -fsSL https://raw.githubusercontent.com/ljheee/claude-supervisor/main/install.sh | sh
```

终端 A：`/rename supervisor`，然后 `/supervisor 做一个XXX功能`。
终端 B（同一项目目录）：`/worker`。

然后该干嘛干嘛，监工会在 worker 中断、静默、跑偏的时候找你。
